# PRD and CLI Deviations Review

Date: 2026-03-26

Scope:
- Compared the implementation in this repository against [docs/PRD.md](docs/PRD.md) and [docs/cli-design.md](docs/cli-design.md).
- Treated code paths wired into the runnable product as authoritative over package-level scaffolding and aspirational docs.
- Used repository code, not README-level intent, to determine what is actually implemented.

Summary:
- The repository has solid building blocks for proposal lifecycle management, Merkle audit logging, Firecracker runtime management, builder orchestration, and encrypted secret storage.
- The main gaps are still end-to-end product wiring and interface accuracy: the live Court and chat flows bypass the PRD sandbox boundaries, the approved-skill pipeline is not connected through deployment, and the published CLI specification is materially ahead of the implemented command surface.

## Deviation List

| ID | Source | Requirement | Current Implementation | Evidence | Deviation |
| --- | --- | --- | --- | --- | --- |
| D1 | PRD | Governance Court reviewers must run in isolated microVMs. | The live Court engine is initialized with `DirectLauncher`, which calls Ollama directly on the host and never creates a reviewer sandbox. The Firecracker launcher exists but is not the active path. | [cmd/aegisclaw/court_cmd.go](cmd/aegisclaw/court_cmd.go), [internal/court/direct_launcher.go](internal/court/direct_launcher.go), [internal/court/reviewer.go](internal/court/reviewer.go) | The primary review path violates the PRD's isolation model for Court reviewers. |
| D2 | PRD, CLI | The main agent should be a sandboxed component, and `aegisclaw chat` should be the primary daily interface into that sandbox. | The chat command runs in the host CLI process, calls Ollama directly from the host, and invokes daemon APIs itself. There is no main-agent microVM in the active user flow. | [cmd/aegisclaw/chat.go](cmd/aegisclaw/chat.go), [cmd/aegisclaw/start.go](cmd/aegisclaw/start.go) | The implemented chat path does not enforce the PRD's main-agent sandbox boundary. |
| D3 | PRD | A newly approved skill should build, deploy, activate in its own microVM, and become immediately usable. | There is a builder runtime and pipeline package, but the CLI exposes only `builder status`, the Court flow does not trigger the pipeline, and the guest agent returns a stub message when no deployed tool exists. | [cmd/aegisclaw/builder_cmd.go](cmd/aegisclaw/builder_cmd.go), [internal/builder/pipeline.go](internal/builder/pipeline.go), [cmd/guest-agent/main.go](cmd/guest-agent/main.go) | The end-to-end skill-addition workflow described by the PRD is still incomplete. |
| D4 | PRD | Skill runtime should execute reviewed, versioned skill artifacts rather than a generic placeholder environment. | Skill activation starts a sandbox from the shared rootfs template and registers it in the runtime registry, but there is no link from an approved proposal, pipeline result, commit, or artifact bundle to the VM that gets activated. | [cmd/aegisclaw/start.go](cmd/aegisclaw/start.go), [cmd/aegisclaw/skill_activate.go](cmd/aegisclaw/skill_activate.go), [internal/builder/pipeline.go](internal/builder/pipeline.go) | Skill activation exists, but it is not tied to reviewed and versioned outputs as required by the PRD. |
| D5 | PRD, CLI | Secrets must be added through dedicated CLI commands without exposing values, then injected at runtime through a secret proxy into real skill execution. | The CLI command is `secret`, not `secrets`, and `secret add <name> <value>` takes the secret value on the command line instead of using a secure prompt. Separately, the vault/proxy and `secrets.inject` guest support exist, but activation and invocation do not resolve or inject proposal-linked secrets. | [cmd/aegisclaw/secret_cmd.go](cmd/aegisclaw/secret_cmd.go), [internal/vault/proxy.go](internal/vault/proxy.go), [cmd/guest-agent/main.go](cmd/guest-agent/main.go), [cmd/aegisclaw/start.go](cmd/aegisclaw/start.go) | Both the CLI contract and the end-to-end secret-delivery path are below the PRD and CLI-design requirements. |
| D6 | PRD, CLI | All proposals, reviews, code changes, deployments, and runtime actions must be covered by the append-only Merkle audit log and exposed through `audit log`, `audit why`, and verification flows. | The Merkle chain is real and verification exists, but the CLI exposes `audit verify` and an interactive `audit explorer`, not `audit log` or `audit why`. Skill invocation is not audit-logged in the daemon path, deployment is not implemented, and there is no structured explanation endpoint built on top of audit records. | [cmd/aegisclaw/root.go](cmd/aegisclaw/root.go), [cmd/aegisclaw/start.go](cmd/aegisclaw/start.go), [cmd/aegisclaw/audit_explorer.go](cmd/aegisclaw/audit_explorer.go), [internal/audit/merkle.go](internal/audit/merkle.go) | Audit infrastructure exists, but PRD-required coverage and CLI-level inspection/explanation capabilities are incomplete. |
| D7 | PRD | Court interactions should use strict schema validation, high structured-JSON reliability, and repeated reviewer consistency checks. | Reviewer responses are parsed into Go structs and lightly validated. `DirectLauncher` requests `Format: "json"`, but there is no JSON Schema enforcement, no three-consistent-output loop per reviewer, and no success-rate instrumentation proving the PRD targets. The reviewer currently cross-checks across configured models with `minModels=2`. | [internal/court/direct_launcher.go](internal/court/direct_launcher.go), [internal/court/reviewer.go](internal/court/reviewer.go), [cmd/aegisclaw/court_cmd.go](cmd/aegisclaw/court_cmd.go) | The Court contract is only partially implemented relative to the PRD's schema-governed and consistency-checked requirements. |
| D8 | PRD | Threat modeling, policy-as-code, SAST, SCA, secrets scanning, and adversarial testing should gate changes. | Some analysis plumbing exists, and sandbox specs validate a few invariants in code, but there is no OPA/Rego policy enforcement, no integrated SAST/SCA gate in the live flow, no secrets scan in the pipeline, and no operational adversarial-test harness wired into approval or deployment. | [internal/builder/analysis.go](internal/builder/analysis.go), [internal/builder/pipeline.go](internal/builder/pipeline.go), [internal/sandbox/spec.go](internal/sandbox/spec.go), [cmd/aegisclaw/builder_cmd.go](cmd/aegisclaw/builder_cmd.go) | Security gates are materially below the PRD baseline. |
| D9 | PRD | Build artifacts and microVM assets should be signed, hashed, SBOM-backed, provenance-tracked, and verified before launch. | Runtime startup provisions Firecracker assets and sandbox startup checks paths, but there is no signature verification, SBOM generation, provenance emission, or launch-time artifact verification path tied to approved builds. | [cmd/aegisclaw/start.go](cmd/aegisclaw/start.go), [internal/sandbox/firecracker.go](internal/sandbox/firecracker.go), [internal/config/config.go](internal/config/config.go) | The supply-chain controls promised in the PRD are not implemented end to end. |
| D10 | PRD, CLI | Deployment should use versioned compositions with rollback and health-based recovery. | Safe mode exists, but there is no composition manifest controller in the runtime path, no health-based deployment switch, and rollback is explicitly unimplemented in the audit explorer. | [cmd/aegisclaw/start.go](cmd/aegisclaw/start.go), [cmd/aegisclaw/audit_explorer.go](cmd/aegisclaw/audit_explorer.go), [docs/cli-design.md](docs/cli-design.md) | Release management and recovery are materially behind both the PRD and CLI design. |
| D11 | PRD, CLI | High-risk actions should require explicit human confirmation, and the system should expose decision explanations. | Skill invocation proceeds directly once the tool call reaches the daemon. There is no typed approval gate for high-risk actions, no per-action confirmation workflow, and no `audit why` or equivalent structured explanation endpoint. | [cmd/aegisclaw/chat.go](cmd/aegisclaw/chat.go), [cmd/aegisclaw/start.go](cmd/aegisclaw/start.go), [cmd/aegisclaw/audit_explorer.go](cmd/aegisclaw/audit_explorer.go) | Human-in-the-loop controls and explanation surfaces are weaker than required. |
| D12 | PRD | The product flow for new skills should use multi-step interactive refinement through the main agent before Court review. | New skill creation is handled by a host-side wizard or CLI flags under `propose skill`, not by a sandboxed conversational main-agent flow that iteratively refines requirements and then routes to Court. | [cmd/aegisclaw/propose_skill.go](cmd/aegisclaw/propose_skill.go), [internal/wizard/wizard.go](internal/wizard/wizard.go), [cmd/aegisclaw/chat.go](cmd/aegisclaw/chat.go) | The current UX is functional, but it is not the product flow described in the PRD. |
| D13 | CLI | The top-level CLI should expose `init`, `start`, `stop`, `status`, `chat`, `skill`, `audit`, `secrets`, `self`, and `version`, with chat as the primary surface and skill management under `skill add/list/revoke/info`. | The implemented top-level commands are `start`, `status`, `sandbox`, `version`, `skill`, `audit`, `propose`, `court`, `builder`, `secret`, `chat`, and `model`. There is no `init`, no `self`, no daemon-level `stop`, and the `skill` subcommands are `activate`, `deactivate`, and `ls` rather than the published contract. | [cmd/aegisclaw/root.go](cmd/aegisclaw/root.go), [cmd/aegisclaw/skill_activate.go](cmd/aegisclaw/skill_activate.go), [cmd/aegisclaw/propose_skill.go](cmd/aegisclaw/propose_skill.go), [cmd/aegisclaw/secret_cmd.go](cmd/aegisclaw/secret_cmd.go) | The implemented CLI surface materially diverges from the published CLI specification. |
| D14 | CLI | `aegisclaw start --safe` should enter a sterile safe-mode environment with a constrained command set, no LLM, no Court, and no chat-driven control flow. | The implemented flag is `--safe-mode`, not `--safe`. Safe mode only toggles a boolean in the daemon and deactivates skills; the normal chat command still exists, the daemon still exposes the full socket API, there is no dedicated safe-mode shell/banner, and the specified `status/logs/revoke/inspect/recover/help` interface is not implemented. | [cmd/aegisclaw/root.go](cmd/aegisclaw/root.go), [cmd/aegisclaw/start.go](cmd/aegisclaw/start.go), [cmd/aegisclaw/chat.go](cmd/aegisclaw/chat.go), [docs/cli-design.md](docs/cli-design.md) | Safe mode exists only as a coarse execution block, not as the distinct recovery interface described by the CLI design. |
| D15 | CLI | Global flags `--json`, `--verbose`, `--dry-run`, and `--force` should be supported consistently, and JSON output should be available on all commands. | The root command does not define those global flags, and many commands print human-oriented text only. Some commands mention JSON in docs or behavior expectations, but the implemented CLI does not provide consistent structured output. | [cmd/aegisclaw/root.go](cmd/aegisclaw/root.go), [cmd/aegisclaw/status.go](cmd/aegisclaw/status.go), [cmd/aegisclaw/skill_activate.go](cmd/aegisclaw/skill_activate.go), [cmd/aegisclaw/secret_cmd.go](cmd/aegisclaw/secret_cmd.go) | The scripting and operator-facing contract in the CLI spec is not implemented. |
| D16 | CLI | `version` and `status` should report build/deployment metadata such as git commit, build date, composition version, health summary, and SBOM summary where available. | `version` prints only the constant `v0.1.0`. `status` reports runtime paths, counts, and registry/audit hashes, but not composition version, deployment health summaries, or SBOM information. | [cmd/aegisclaw/root.go](cmd/aegisclaw/root.go), [cmd/aegisclaw/version.go](cmd/aegisclaw/version.go), [cmd/aegisclaw/status.go](cmd/aegisclaw/status.go) | The implemented operational introspection is narrower than the published CLI contract. |

## Observations That Reduce Risk But Do Not Close Gaps

- Proposal lifecycle management is well structured in [internal/proposal/proposal.go](internal/proposal/proposal.go).
- Firecracker runtime support is substantial in [internal/sandbox/firecracker.go](internal/sandbox/firecracker.go), so several isolation gaps are wiring and guest-protocol gaps rather than complete absence of sandbox machinery.
- The Merkle audit chain is a real signed append-only log in [internal/audit/merkle.go](internal/audit/merkle.go).
- Builder/runtime abstractions are far enough along that the main missing work is integration into approval, deployment, and runtime activation rather than greenfield subsystem creation.
- The vault and proxy design in [internal/vault/vault.go](internal/vault/vault.go) and [internal/vault/proxy.go](internal/vault/proxy.go) are directionally consistent with the PRD, but they are not yet enforced by the live invocation path.

## Most Important Root Causes

1. The live product path still favors host-side fallbacks over PRD-mandated sandbox boundaries.
2. Proposal, Court, builder, activation, and runtime subsystems were implemented as separate capabilities but not connected into one enforced workflow.
3. Supply-chain, policy, and explanation requirements were modeled conceptually but not yet turned into launch-time or operator-facing enforcement.
4. The published CLI design is aspirational relative to the current implementation, so the docs currently promise workflows and interfaces that the binary does not yet deliver.